type: object
required:
  - name
  - email
  - type
  - creator
  - status
properties:
  name:
    type: string
  email:
    type: string
  type:
    type: string
    enum: [customer, admin]
  creator:
    type: string
  status:
    type: string
    enum: [disabled, active]

